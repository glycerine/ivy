// Code generated by "go test -run=Generate -write=all"; DO NOT EDIT.
// Source: ../../cmd/compile/internal/types2/tuple.go
import { TypeString } from "./typestring.js";
// A Tuple represents an ordered list of variables; a nil *Tuple is a valid (empty) tuple.
// Tuples are used as components of signatures and to represent the type of multiple
// assignments; they are not first class types of Go.
export class Tuple {
    vars;
    constructor(vars) {
        this.vars = vars;
    }
    // Len returns the number variables of tuple t.
    Len() { return this.vars.length; }
    // At returns the i'th variable of tuple t.
    At(i) { return this.vars[i]; }
    Underlying() { return this; }
    String() { return TypeString(this, null); }
}
// NewTuple returns a new tuple for the given variables.
export function NewTuple(...x) {
    if (x.length > 0) {
        return new Tuple(x);
    }
    return null;
}
