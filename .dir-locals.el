((nil . ((eval . (let ((venv-path (expand-file-name "~/go/src/github.com/glycerine/goivy/pyivy/goivy-venv/bin")))
                   (setq-local process-environment (cons (concat "PATH=" venv-path ":" (getenv "PATH")) process-environment))
                   (setq-local exec-path (cons venv-path exec-path)))))))

